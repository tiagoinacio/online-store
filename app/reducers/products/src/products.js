import products from '../../../api/products.json';

/**
 * Products Reducer
 * Fetch the products from local json file
 *
 * @name ProductsReducer
 * @function
 * @returns {Array} array of products
 */
export const ProductsReducer = () => products;
